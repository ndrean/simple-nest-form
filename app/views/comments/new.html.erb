<h1>New Comment for Post # <%= @post.id %> (with custom "save")</h1>


<%= simple_form_for([@post,@comment], url: post_comments_save_path(@post)) do |f| %>
<%#= simple_form_for [@post,@comment], url: {controller: 'comments', action: "save"} do |f| %>
  <%= f.input :body %>
  <%= f.button :submit, class:"btn btn-primary" %>
<% end %>
    
<pre>
# comments#new
def new
  @post = Post.find(params[:post_id]) # <=> before_action :set_post
  @comment = @post.comments.build
end
</pre>
<pre>
#views/comments/new.html.erb
&lt%= form_with(model: [@post, @comment], local: true) do |form| %>

    &lt%= render 'comments/form', form: form %>
  
&lt% end %>
</pre>
<%= link_to 'Back to Comments of the post', post_comments_path, class:"badge badge-info", style:"color:white;font-size:14px;" %>
